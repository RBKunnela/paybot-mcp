# Semantic Intent — BP-1.6

EPIC-BANK-3 (HITL middleware + 5 new MCP tools) starts from a clean paybot-mcp working tree with a validated vitest scaffold, so the upcoming tool-contract tests have a real runner to land in. Bank reviewers and open-source-conscious bank IT teams reading the public Apache-2.0 repo see a coherent, recent commit history — not a single dump of untracked files — when they audit the project before signing the pitch engagement. The README's example facilitator URL points to the production endpoint (`api.paybotcore.com`), so any reviewer copy-pasting the config block lands on the real service instead of a dev placeholder. No secrets, no operator-private data, no internal harness references, and no proprietary AML/KYC vendor names leak into the public repo through this commit. The MCP capability-gap analysis (`AGENT-ECONOMY-MCP-GAPS.md`) is in the repo so the next engineer to touch this codebase has the roadmap context for the next 6 weeks of work.
